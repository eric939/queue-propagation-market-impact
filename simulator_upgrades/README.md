# Simulator Upgrades

`simulator_upgrades/` contains small simulator-extension modules and upgrade
experiments that are not part of the stable `limit_order_book/` core.

Promote reusable mechanics into `limit_order_book/` only when they become stable
dependencies.
