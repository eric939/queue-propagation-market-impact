# Limit Order Book Core

`limit_order_book/` contains the reusable event-level LOB implementation.

This layer should stay independent of paper-specific plotting, manuscript
structure, and generated artifact paths.
